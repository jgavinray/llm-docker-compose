# LLM Docker Compose

Machine-scoped Docker Compose deployments for local LLM services.

## Layout

- `spark/`: DGX Spark host at `192.168.0.33`.

Each machine directory should contain the Compose files used on that host.
